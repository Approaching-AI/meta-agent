# 2026-03-26

## Session 18:24

operator: xwy

**做了什么**：
- 复查了当前 `sop-new/` 结构、`SOURCE-MAPPING.md`、5 篇 K2.5 相关 daily notes，以及 `deploy-scripts/` 下的 K2.5 方案目录和 `deploy-scripts/tools/pack_bundle.sh`。
- 判断本轮 handoff 指定的 K2.5 材料暂时不适合硬拆成新的 `20-deployment` playbook：大部分内容仍是一次性 bring-up、SLO 数字、可行性汇报和 benchmark 运行记录。
- 新增 `sop-new/reference/09-runtime-image-and-deploy-bundle-reference.md`，把 runtime image、deploy bundle、方案目录三层边界写清楚，并把 `pack_bundle.sh` 纳入来源。
- 更新 `sop-new/INDEX.md`、`sop-new/00-system-overview.md`、`sop-new/20-deployment/21-deployment-scheme-and-service-start.md`、`sop-new/SOURCE-MAPPING.md`，补上新 reference 入口，并把本轮 K2.5 triage 结论登记为“保留为项目日志”或“已吸收”。
- 运行了 `sop-new/` 下 markdown 相对链接检查，结果通过。

**结论**：
- 当前更稳定、可复用的增量不是再写一篇 K2.5 方案 SOP，而是先把“镜像基线 vs bundle 增量 vs 方案目录”这条边界补齐，避免后续继续把环境固化、代码交付和现场配置混写。
- `daily-notes/2026-03-10-pp2-pp4-decode-sla-long-input.md` 的方法层内容已经足够被 `20-deployment/22` 吸收，但其余本轮重点查看的 K2.5 材料仍应保留为项目日志或后续参考专题来源。

**待处理 / 风险**：
- `sop-new/` 里仍缺一类可能有价值的 reference：K2.5 方案比较/选型边界。如果后续继续做，应该写成 reference，而不是把一次性 benchmark 数字包装成 SOP。
- 当前工作区有 autonomous driver 产生的 `.meta-agent/`、`handoff-run/`、`handoff-history/` 等运行态改动；commit 时需要继续精确控制文件集合。

**下一步**：
- 已创建 `handoff/2026-03-26-sop-new-k25-reference-triage-next.pending.md`，交给后续 session 继续判断是否需要新增“K2.5 方案比较 reference”，并顺手补一轮部署/benchmark 文档中的高价值命令示例。

## Session 18:29

operator: xwy

**做了什么**：
- 继续阅读 5 篇 K2.5 候选 daily notes 与 `deploy-scripts/3p6d-k25-pp2-ep48-mtp/`、`7p-pp4pp3-4d-k25-ep32-mtp/`、`8p-2xpp4-4d-k25-ep32-mtp/` 的 `cluster.conf` / `launch_*.sh` / `start_all.sh`，确认这批材料里存在稳定的“方案选型边界”，而不是新的 step-by-step playbook。
- 新增 `sop-new/reference/10-k25-scheme-selection-reference.md`，把 `PP2` / `TP16` / 多组 PP / `EP32` / `EP48` 的默认选择顺序、`PREFILL_ENDPOINTS` 约束、长上下文优先补 prefill、以及引擎代际污染 benchmark 结论等边界整理成 reference。
- 更新 `sop-new/INDEX.md`、`sop-new/00-system-overview.md`、`sop-new/20-deployment/21-deployment-scheme-and-service-start.md`、`sop-new/20-deployment/22-benchmark-and-capacity-validation.md`、`sop-new/SOURCE-MAPPING.md`，补上新 reference 入口，并把本轮 K2.5 方案材料从“延期”改为“已吸收为 reference / SOP 补强”。
- 顺手修正了 `20-deployment/21` 里的健康检查示例，去掉对 `30000` / `8000` 的硬编码，改成使用 `ROUTER_PORT` / `SERVER_PORT`，避免与 `30080` 这类变体方案冲突。
- 运行了 `sop-new/` 相对链接检查，结果通过。

**结论**：
- 本轮 K2.5 候选材料值得沉淀，但应写成 reference，而不是再造一篇新的 `20-deployment` 方案 SOP。
- 当前更稳定的跨材料共识是：K2.5 decode 默认优先保持 `EP32`，高并发容量边界才考虑 `EP48`；长上下文问题先比较是否增加 prefill 组数；多节点 prefill 在 PD 分离下必须显式把 router endpoint 收敛到各组 master。
- 7P / 4P 之类方案对比在引擎版本、`MLAPO`、MTP / EAGLE3、draft 权重和 graph 策略不一致时，只能作为趋势参考，不能直接写成拓扑优劣结论。

**待处理 / 风险**：
- `sop-new/` 仍有一个明确延期项：`daily-notes/2026-03-11-ascend-cluster-procurement-guide.md`。下一轮需要判断它是否值得写成一篇窄范围、repo-local 的 hardware/procurement reference；如果不能稳定复用，就只更新 `SOURCE-MAPPING.md`。
- 工作区仍存在 autonomous driver 产生的 `.meta-agent/`、`handoff-run/`、`handoff-history/` 等运行态改动；commit 时继续只纳入本轮真正新增的文档、日志和 handoff。

**下一步**：
- 已创建 `handoff/2026-03-26-sop-new-procurement-or-source-mapping-next.pending.md`，交给后续 session 优先 triage `2026-03-11-ascend-cluster-procurement-guide.md`，若不值得成文则退回到 `SOURCE-MAPPING.md` 的覆盖扩展。

## Session 18:34

operator: xwy

**做了什么**：
- 阅读并重新判断了 `daily-notes/2026-03-11-ascend-cluster-procurement-guide.md`，确认它更适合沉淀为窄范围 reference，而不是继续延期或混入部署 SOP。
- 新增 `sop-new/reference/01-ascend-cluster-hardware-and-procurement-reference.md`，把本仓库可复用的硬件基线、RoCE 网络边界、管理网/存储要求、驱动/固件/TLS 一致性和交付验收清单整理成 repo-local reference。
- 更新 `sop-new/INDEX.md`、`sop-new/00-system-overview.md`、`sop-new/SOURCE-MAPPING.md`，补上新 reference 入口，并把 `2026-03-11-ascend-cluster-procurement-guide.md` 从“延期”改为“已吸收”。
- 运行了 `sop-new/` 内 markdown 相对链接检查，结果通过。

**结论**：
- `2026-03-11-ascend-cluster-procurement-guide.md` 的主体内容足够稳定，适合保留为“硬件采购 / 租用 / 交付验收边界 reference”；不需要因为它带有采购背景就整体留在 daily notes。
- 这类内容最合适的落点是 reference，而不是 `10-cluster/` 或 `20-deployment/` 的 step-by-step SOP；它回答的是“什么样的硬件值得纳入本仓库流程”，不是“如何启动某个方案”。
- 当前 `sop-new/` 主线已经较完整，下一轮更值得做的是补强 `SOURCE-MAPPING.md` 的审计性，明确剩余 3 月 daily notes 是“已吸收”还是“保留为项目日志”。

**待处理 / 风险**：
- `sop-new/SOURCE-MAPPING.md` 仍未覆盖一批 2026-03 daily notes，其中有些可能已经被现有 SOP/reference 实质吸收，只是映射未补齐。
- 工作区仍存在 autonomous driver 产生的 `.meta-agent/`、`handoff-run/`、`handoff-history/` 以及 handoff 目录状态变更；commit 时仍需只纳入本轮新增文档、日志和新的 handoff。

**下一步**：
- 已创建 `handoff/2026-03-26-sop-new-march-triage-next.pending.md`，交给后续 session 优先审计未映射的 2026-03 daily notes，优先补 `SOURCE-MAPPING.md` 的 triage 结论，只有在确有新的高复用 stable topic 时才新增 reference。

## Session 18:52

operator: xwy

**做了什么**：
- 继续按 handoff 约束复查 runtime image / deploy bundle 主题，只从 `docs/sop/1.4-image-snapshot-distribute.md` 和 `daily-notes/2026-02-11-cluster-expansion.md` 中提取出唯一仍值得保留的稳定约束：大镜像分发前必须确认 Docker `data-root` 不落在小系统盘。
- 更新 `sop-new/10-cluster/11-cluster-baseline-and-onboarding.md`，补入 `Docker Root Dir` 预检，并把这条约束放回“集群基线”而不是 `reference/09`，避免继续扩写已经收敛的镜像迁移主题。
- 新增 `sop-new/10-cluster/14-driver-and-firmware-upgrade.md`，把仍只存在于旧 `docs/sop/2.2` 和 2 月/3 月 daily notes 中的驱动固件升级流程收敛进 `sop-new/` 主线；同时更新 `sop-new/INDEX.md` 和 `10-cluster/11` 的内部跳转，不再让新体系把这类基线流程回退到旧 SOP。
- 更新 `sop-new/SOURCE-MAPPING.md`，补记 `2026-02-05-driver-upgrade.md`、`2026-02-05-install-npu-driver-firmware.md`、`2026-02-11-cluster-expansion.md`、`2026-02-12-sino-qj201-standardization.md`、`2026-02-27-benchmark-methodology-fix.md` 以及旧 `docs/sop/1.4`、`docs/sop/2.2` 的吸收去向。
- 运行了 `sop-new/` 内相对 Markdown 链接检查，结果通过。

**结论**：
- runtime image / deploy bundle / 权重分发这条边界目前确实已经收敛，不值得再为了对称性新增 reference；唯一新增的稳定约束是 Docker `data-root`，且它属于集群纳管前置检查。
- `sop-new/` 之前仍缺一篇高复用的“驱动/固件升级” SOP，导致集群基线文档还要回跳到旧 `docs/sop/2.2`。这一缺口现在已经补上。
- 部分 2 月 daily notes 其实已经被现有 cluster / benchmark 文档吸收，只是 `SOURCE-MAPPING.md` 未登记；mapping-first 仍然是当前最有效的推进方式。

**待处理 / 风险**：
- `SOURCE-MAPPING.md` 仍有不少 2026-02 daily notes 未 triage，尤其是早期 benchmark 对比、节点扩容和方案试验记录；这些材料不一定都要新增文档，但需要更明确地标为“已吸收”或“保留为项目日志”。
- 工作区仍存在 driver 管理的 `handoff-run/`、`handoff-history/`、`.meta-agent/` 等运行态变化；提交时必须继续精确 staging，避免把这些状态文件误入 commit。

**下一步**：
- 已创建 `handoff/2026-03-26-sop-new-feb-triage-next.pending.md`，交给后续 session 继续审计剩余未映射的 2026-02 daily notes，优先补 `SOURCE-MAPPING.md`，只有在确实发现新的高复用稳定主题时才新增 SOP/reference。
